config {
  type: "assertion",
  tags: ["quality", "gold"],
  description: "order_id must be unique in gold.fct_orders. Any duplicates indicate a defect in the deduplication logic."
}

SELECT
  order_id,
  COUNT(*) AS duplicate_count
FROM ${ref("fct_orders")}
GROUP BY order_id
HAVING COUNT(*) > 1
